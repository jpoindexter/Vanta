# Rust
/target

# Node
node_modules
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Runtime data dirs (state, not source)
.vanta/
.nexarion/

# CodeGraph index (rebuildable from source)
.codegraph/

# Generated artifacts
roadmap.html

# OS
.DS_Store

# External agent source cloned for audit (not vendored)
reference/
argo
.argo/

# Transient agent worktrees (embedded git repos — never commit)
.claude/worktrees/
