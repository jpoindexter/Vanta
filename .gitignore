# Rust
/target

# Node
node_modules
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Local MCP config (machine-specific server paths); .mcp.json.example is the template
.mcp.json

# Runtime data dirs (state, not source)
.vanta/
.nexarion/

# CodeGraph index (rebuildable from source)
.codegraph/

# Generated artifacts
roadmap.html

# OS
.DS_Store

# External agent source cloned for audit (not vendored) — anchored to repo root so the
# bare pattern doesn't also drop vanta-website/docs/reference (real site content) from git
/reference/
/docs/reference/
argo
.argo/

# Transient agent worktrees (embedded git repos — never commit)
.claude/worktrees/

# Agent scratch space (OCR/temp output from running Vanta in this repo)
.vanta_tmp/
tmp/

# Internal dev artifacts — not for the public repo (machine config, handoffs, planning docs)
.codex/
HANDOFF.md
docs/superpowers/
docs/desktop-completion-plan.md
