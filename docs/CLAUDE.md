# Documentation

Product, verification, and release documentation for Vanta.

- Release notes live in `releases/` and summarize only shipped, evidenced behavior.
- `CHANGELOG.md` at the repository root remains the canonical chronological changelog.
