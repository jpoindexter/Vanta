# Integrations

The integration catalog is the single state model for CLI, TUI, and Desktop. `receipts.ts` owns durable redacted outcome records. Provider adapters accept an injected `fetch` for contract tests; actual credentials are read only from explicitly named `VANTA_*` environment variables.
