# Schema task environments

Use `TaskEnvironment` for a bounded, replayable task substrate. Use `TaskTransitionTimeline` for append-only, redacted transition events on the kernel audit chain. Use `groundTransition` and immutable revisions for interpreted task state. Keep live browser, repository, operator, and cryptographic verification I/O behind injected callers.
